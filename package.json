{
  "name": "rabbit-wallpaper",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "homepage": ".",
  "dependencies": {
    "antd": "^3.21.4",
    "axios": "^0.21.1",
    "moment": "^2.24.0",
    "puppeteer": "^1.19.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "wallpaper": "^4.4.1"
  },
  "scripts": {
    "start1": "react-scripts start",
    "start2": "cross-env DEV_MODE=true electron .",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "react-build": "react-scripts build",
    "electron-build": "electron-packager . RabbitWallPaper --overwrite --platform=win32 --out ./dist --arch=x64 --app-version=0.0.1 --electron-version=5.0.7",
    "build": "yarn react-build && yarn electron-build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "electron": "^5.0.7",
    "cross-env": "^5.2.0",
    "electron-packager": "^13.1.1"
  }
}
